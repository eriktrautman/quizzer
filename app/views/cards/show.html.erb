<div id="show-card">
  <h1> Taking Quiz: <%= @quiz.name %> </h1>
  <%= render :partial => "pass_fail_show", :locals => { :quiz => @quiz } %>

  <div class="card-category">
    <h3>Category: <%= @card.category.name %></h3>
  </div>

  <div class="card-title">
    <h2><%= @card.title %></h2>
  </div>

  <div class="card-body">
    <%= md(@card.body).html_safe %>
  </div>

  <%= render :partial => "pass_fail_show", :locals => { :quiz => @quiz } %>


</div>