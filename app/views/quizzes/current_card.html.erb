<div id="current-card">
  <h1> Taking Quiz: <%= @quiz.name %> </h1>

  <div class="card-category">
    <h3>Category: <%= @current_card.category.name %></h3>
  </div>

  <div class="card-title">
    <h2><%= @current_card.title %></h2>
  </div>

  <div class="card-body">
    <%= md(@current_card.body).html_safe %>
  </div>


</div>